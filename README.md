Frontend dashboard project for managing users, lessons, quizzes, and analytics.  
Hiện đang dùng **mock data** trong `firebaseStats.services.js` để frontend hoạt động. Có thể kết nối backend thật sau này với Firestore/Cloud Functions.
Muốn Code BE thì trong firebaseStats.services.js hãy xóa từ dòng 264 tới hết và code BE ở đoạn code phía trên

---

## 🚀 Bắt đầu

1. Clone repository:

```bash
git clone https://github.com/tranhuuthang369/FE_DashBoard.git

cd FE_DashBoard

npm install

npm run dev


